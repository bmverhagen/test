# Bux Zero access token (verkrijg via: python3 -m bux get-token)
BUX_TOKEN=

# Optioneel: map ISIN naar Yahoo Finance ticker via OpenFIGI
OPENFIGI_API_KEY=

# Output directory voor opgeslagen data
OUTPUT_DIR=./data

# Rate limit tussen API calls (seconden)
REQUEST_DELAY=0.3
